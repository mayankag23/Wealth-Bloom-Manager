class Account:
  def __init__(self, account_id, user_id, password, admin, amount):
    self.account_id = account_id
    self.user_id = user_id
    self.password = password
    self.admin = admin
    self.amount = amount
